# Dependencies
node_modules/

# Build output (built on the server during deploy)
dist/

# Sensitive / environment
.env
.env.*

# Deployment (contains repo URL, email, server config)
deploy.sh

# Server / infra
nginx/
certs/
